import React from 'react'

const Header = ({title}) => {
    return (
        <header>
        <h1 style={{ color:'yellow'}}>{title}</h1>
       
        
       
        </header>
    )
}

export default Header
